node_modules
npm-debug.log
Dockerfile*
docker-compose*
.dockerignore
.git
.gitignore
README.md
LICENSE
.vscode
.next
*.swp

# Environment files — injected at runtime via docker-compose `env_file:`,
# never bake them into the image.
.env
.env.*
!.env.example

 # Dev DB bind-mount and runtime uploads — never build inputs
  .postgres_data
  public/uploads
  playwright-report
  test-results
  coverage