/**
 * Class Scheduling Feature
 *
 * Manages weekly class schedules with conflict detection for:
 * - Sections (a section can only be in one place at a time)
 * - Teachers (a teacher can only teach one class at a time)
 * - Rooms (a room can only host one class at a time)
 */

// Schemas
export * from "./schedules.schema";

// Queries (server-only, import directly in server components)
// export * from "./schedules.queries";

// Actions (server-only, import directly in client components)
// export * from "./schedules.actions";

// Components (will be added in Phase 3)
// export * from "./components";
